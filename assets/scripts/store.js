'use strict'

const store = {
  // Ui.js - nextMove- store.last round captures the reponse with all of the board info including the most recent move
  //  Ui.js - newGameStart - stores the starting information when a new game is created
  //  events.js - updateGame - stores the value, or index, of the most recent move
}

module.exports = store
